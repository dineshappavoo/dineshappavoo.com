import { Companies } from "@/components/home/Companies";

export default function CompaniesPage() {
  return (
    <main style={{ paddingTop: '6rem', background: 'var(--bg-primary)' }}>
      <Companies />
    </main>
  );
}